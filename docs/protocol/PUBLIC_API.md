# @aoc/protocol Public API

This is the governed list of every export path `@aoc/protocol` publishes, generated from the actual
barrel files under `packages/protocol/src/`. It supersedes ad-hoc inspection of source — if a symbol
isn't listed here, treat it as internal even if it happens to be reachable via a deep import (deep
imports are unsupported and actively blocked, see `scripts/assert-invalid-imports.mjs`).

## Governance policy

- Every public symbol must be reachable through one of the barrels below (`packages/protocol/src/*/index.ts`)
  and declared in `packages/protocol/package.json` `exports`. No other import path is supported.
- Deep imports (`@aoc/protocol/dist/...`, `@aoc/protocol/src/...`, `@aoc/protocol/internal/...`) are
  not supported, regardless of whether they happen to resolve in a given bundler.
- Removing or renaming a stable public export requires a Changeset classified `major` and an entry in
  this document's history.
- Additive, backwards-compatible exports (new fields, new subpaths, new deprecated-but-present aliases)
  require a Changeset classified `minor`.
- Fixes that don't change any exported contract shape require a Changeset classified `patch`.
- Experimental subpaths (`adapters`, `runtime-registry`) may change shape without a major bump while
  they remain marked experimental in `docs/versioning-and-stability.md`, but any such change still
  requires a Changeset (minor or patch) documenting the change.
- Internal APIs (anything not in this table) must never be exported accidentally — this is enforced by
  `__tests__/architecture/protocol-purity.test.ts` asserting the barrel `index.ts` files are the only
  public entry points and by the tarball-content check in `scripts/validate-protocol-consumer.mjs`.

## Export table

| Export path | Symbols | Stability | Runtime/type | Since | Notes |
| --- | --- | --- | --- | --- | --- |
| `@aoc/protocol` (root) | Alias of `./contracts` | Stable | Type-only | 0.1.0 (root export added this sprint) | `"."` in `exports` was added as part of the consumer-readiness sprint; no symbol changes |
| `@aoc/protocol/contracts` | `CanonicalId`, `UtcDateTime`, `ResourceRef`, `Delegation`, `Constraint`, `ProofMetadata`, `CapabilityToken`, `CapabilityGrant`, `AgentScope`, `ContextCondition`, `ConsentGrant`, `PolicyDecision`, `ScopedAccessRequest`, `AuditEventEnvelope`, `TrustDomainIdentifier` | Stable | Type-only | 0.1.0 (`AuditEventEnvelope`'s `occurredAt`/`subject`/`correlationId`/`reasonCodes`/`schemaVersion` fields added in the Public API Stabilization Sprint, additive/minor) | Also re-exports `legacy-contracts` types (compatibility-only, deprecated). `ScopedAccessRequest.requestedScope` is and has always been the sole scope field — there is no `scope`/`action` alias. |
| `@aoc/protocol/errors` | `ProtocolError` and related error types (`protocol-error`), plus `legacy-errors` | Stable | Type-only | 0.1.0 | Pure re-export barrel |
| `@aoc/protocol/claims` | RFC-005 claim family: `primitives`, `claim-enums` (`ClaimType`, `EvidenceType`, `AttestationType`, `VerificationStatus`, `StandingStatus`, `AuthorityStatus`, `DecisionStatus`), `references`, `proofs/*`, `registries/*`, `credentials/*`, `vocabulary/*`, `CanonicalClaim`, `CanonicalAttestation`, `CanonicalVerification`, `CanonicalStanding`, `validateCanonicalStanding`, `isValidCanonicalStanding`, `CanonicalStandingValidationResult`, `CanonicalCapability`, `CanonicalAuthority`, `CanonicalDecision` | Stable | Mixed — `claim-enums` and `legacy-claims` constants are runtime values; contract shapes are types | 0.1.0 (`ClaimType.Derivation` added in SM-05; `CanonicalStanding` runtime validators added in SM-06 — both additive/minor) | Also declares deprecated `LegacyClaim`/`Claim` locally and re-exports `legacy-claims`. `ClaimType` gained the additive `Derivation` member for first-class derivation assertions (`@aoc/protocol/manifest`'s `DerivationClaim`); no existing `ClaimType` value was removed, renamed or re-spelled. SM-06 added the first runtime structural validators for `CanonicalStanding` — the type was previously type-only — because a portability bundle carries standing records across an external trust boundary; they validate shape only and never adjudicate a dispute, order timestamps or resolve a `claimRef` |
| `@aoc/protocol/adapters` | `AdapterResult<T>`, `AdapterLookupContext`, `VerificationKeyDescriptor`, `VerificationKeyResolver`, `RevocationStatus`, `RevocationLookup`, `RegistryLookup`, `TrustRegistryProvider`, `CapabilityLookup`, `AttestationLookup`, `CredentialStatusLookup`, `AuditEventSink`, `SecurityEvent`, `SecurityEventSink`, `ProtocolEvent`, `ProtocolEventSink`, `PolicyDecisionRequest`, `PolicyDecisionResult`, `PolicyDecisionProvider`, `GovernanceDecisionProvider`, `ExecutionAuthorizationRequest`, `ExecutionAuthorizationResult`, `ExecutionAuthorizationProvider`, `VerificationProvider`, `ObservabilityEventSink` | Experimental | Type-only (interfaces) | Added post-0.1.0 (see `docs/architecture/protocol-export-surface-report.md`) | Also declares deprecated aliases `AccessVerificationPort`, `PolicyEvaluatorPort`, `TrustCoordinationPort`, `TrustDomainPort` |
| `@aoc/protocol/runtime-registry` | `AdapterRegistry`, `RuntimeAdapterBootstrap`, `RuntimeBootstrapEngine`, `createAdapterToken`, `AdapterTokens`, `AllAdapterTokens`, `RuntimeProfileValidationMode`, `RuntimeBootstrapStatus`, `RegisteredAdapterStatus`, `RuntimeAdapterBootstrapStatus`, `AdapterRegistryEventType`, `AdapterNotRegisteredError`, `AdapterAlreadyRegisteredError`, `RegistryValidationError` | Experimental | Runtime (classes, frozen const objects, error classes) | Added post-0.1.0 | The only subpath with real executable runtime behavior beyond enum constants |
| `@aoc/protocol/identity` | `SovereignAssetId`, `mintSovereignAssetId`, `parseSovereignAssetId`, `isValidSovereignAssetId`, `assertValidSovereignAssetId`, `ContentIdentity`, `ContentDigestAlgorithm`, `computeContentIdentity`, `isValidContentIdentity`, `contentIdentitiesEqual`, `verifyContentIdentity`, `contentIdentityKey` | Stable, expanding | Mixed | Added post-0.1.0 | Independent UUID asset identity and exact-byte SHA-256 identity; neither contains storage semantics |
| `@aoc/protocol/canonical` | `CANONICAL_JSON_PROFILE`, `canonicalizeJSON` | Stable, expanding | Runtime | Added post-0.1.0 | Deterministic `aoc-canonical-json/1` representation; unsupported profiles fail manifest validation |
| `@aoc/protocol/manifest` | `SovereignManifestV1`, `SignedSovereignManifest`, `SovereignAssetRegistry`, manifest/claim/proof types, builders and validators, `computeManifestDigest`, `generateSovereignKeyPair`, `signSovereignManifest`, `verifySovereignManifest`, `resolveSovereignAsset`, `resolveSovereignAssetVersion`, `DerivationClaim`, `DerivationRelationKind`, `DERIVATION_RELATION_KINDS`, `BuildDerivationClaimInput`, `buildDerivationClaim`, `validateDerivationClaim`, `isValidDerivationClaim`, `validateOriginClaim`, `isValidOriginClaim`, `validateAuthorityClaim`, `isValidAuthorityClaim`, `ClaimValidationResult`, `traceSovereignLineage`, `SovereignLineageTrace`, `SovereignLineageNode`, `SovereignLineageEdge`, `SovereignLineageDirection`, `SovereignLineageTraceSchemaVersion`, `TraceSovereignLineageInput`, `TraceSovereignLineageValidationResult`, `SOVEREIGN_LINEAGE_DIRECTIONS`, `SOVEREIGN_LINEAGE_TRACE_SCHEMA_VERSION`, `DEFAULT_SOVEREIGN_LINEAGE_MAX_DEPTH`, `validateTraceSovereignLineageInput`, `isValidTraceSovereignLineageInput`, `isValidSovereignLineageMaxDepth`, `verifySignedSovereignClaim`, `SovereignClaimVerificationResult`, `SovereignClaimVerificationChecks`, `VerifySignedSovereignClaimOptions`, `VerifiableSovereignClaim` | Stable, expanding | Mixed | Added post-0.1.0 (derivation claims and lineage traversal added in SM-05; signed-claim verification reporting added in SM-08 — both additive/minor) | Versioned canonical manifests, Ed25519 proofs, verification results, a storage-neutral version-preserving registry port, and first-class derivation assertions with pure ancestor/descendant lineage traversal. Lineage is a claim: `SovereignManifestV1` has no `parentId` or derivation field, a `DerivationClaim`'s `subject` is the child and its sources are `SovereignAssetId`s, and nothing — licence, rights, authority, authorship, evidence — is inherited along an edge. `traceSovereignLineage` is pure over caller-supplied claims: no database, index, provider or graph library, deterministic ordering, honest `cycleDetected`/`truncated`. SM-08 added `verifySignedSovereignClaim`, the additive companion to `verifySovereignManifest`: it reports `claimStructure`, `claimDigest`, `signature` and a three-state `issuerBinding` as separate outcomes rather than one boolean, reusing `verifySignedClaim` for the digest/signature half and the existing `validateOriginClaim`/`validateAuthorityClaim`/`validateDerivationClaim` validators for the structural half, and preserving their reason codes verbatim. It is purely additive — `verifySignedClaim`, `verifySovereignManifest` and `verifySovereignSignature` are unchanged, and every signing primitive (`signClaim`, `signSovereignManifest`, `signSovereignPayload`, `generateSovereignKeyPair`) remains public with unchanged semantics. `VerifiableSovereignClaim` is a type alias over the three existing canonical claim interfaces, not a second claim model. A valid signature proves the issuer asserted the claim, never that it is true, authorized, owned or uncontested; nothing here reads or writes claim standing. See `docs/architecture/sovereign-asset-core.md` §16 |
| `@aoc/protocol/portability` | `SOVEREIGNTY_PORTABILITY_BUNDLE_SCHEMA_VERSION` (`aoc-sovereignty-portability-bundle/1`), `SovereigntyPortabilityBundleV1`, `SovereigntyPortabilityBundleSchemaVersion`, `PortableSovereignManifestArtifact` (`PortableUnsignedSovereignManifestArtifact` | `PortableSignedSovereignManifestArtifact`), `PortableSovereignClaim`, `PortableSovereignClaimArtifact` (`PortableUnsignedSovereignClaimArtifact` | `PortableSignedSovereignClaimArtifact`), `PORTABLE_SOVEREIGN_MANIFEST_ARTIFACT_KINDS`, `PORTABLE_SOVEREIGN_CLAIM_ARTIFACT_KINDS`, `PortableSovereignManifestArtifactKind`, `PortableSovereignClaimArtifactKind`, `BuildSovereigntyPortabilityBundleV1Input`, `buildSovereigntyPortabilityBundleV1`, `tryBuildSovereigntyPortabilityBundleV1`, `SovereigntyPortabilityBundleBuildResult`, `validateSovereigntyPortabilityBundleV1`, `isValidSovereigntyPortabilityBundleV1`, `SovereigntyPortabilityBundleValidationResult`, `isPortableSovereignManifestArtifact`, `isPortableSovereignClaimArtifact`, `portableManifestOf`, `portableClaimOf`, `serializeSovereigntyPortabilityBundle`, `parseSovereigntyPortabilityBundle`, `SovereigntyPortabilityBundleParseResult`, `SOVEREIGNTY_PORTABILITY_REASON_CODES`, `SovereigntyPortabilityReasonCode` | Stable | Mixed — frozen constant data, pure builders/validators, and the canonical serializer/parser | Added post-0.1.0 (new subpath added in SM-06, additive/minor) | The canonical sovereign portability bundle: a versioned, deterministic, provider-neutral representation of one `SovereignSubjectRef` plus the manifest, claim and standing artifacts a caller supplied for it. Six envelope fields and no more — deliberately no `bundleId`, no `exportedAt`, no bundle digest/hash/signature, no provider/storage/tenant state, no content bytes, no completeness flag and no licence/ownership/governance semantics. Serialization is the existing `aoc-canonical-json/1` and no second canonicalizer, archive format, compression or encryption is introduced. Envelope arrays are canonically ordered (manifests by `manifestVersion`, claims by claim `id`, standings by `id`), caller arrays are never mutated and nested artifacts are never rewritten, re-signed, re-digested or normalized. `parseSovereigntyPortabilityBundle` is an external trust boundary and fails closed — including on a *future* bundle schema or an unknown artifact kind, which are rejected rather than best-effort imported or silently dropped. Structural validity is reported as `valid`, never `verified`: nothing here signs, verifies, hashes, resolves an `evidenceRef` or dereferences a locator. Runtime dependencies are `@aoc/protocol/canonical`, `/identity`, `/manifest` and `/claims`; no import-time side effects. See `docs/protocol/SOVEREIGNTY_CAPABILITIES.md` |
| `@aoc/protocol/interoperability` | `SOVEREIGNTY_INTEROPERABILITY_PROFILE_SCHEMA_VERSION` (`aoc-sovereignty-interoperability-profile/1`), `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_ID`, `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_VERSION`, `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_V1`, `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_REF`, `AOC_SOVEREIGNTY_PORTABILITY_MEDIA_TYPE`, `SovereigntyInteroperabilityProfileV1`, `SovereigntyInteroperabilityProfileSchemaVersion`, `SovereigntyInteroperabilityProfileRef`, `SovereigntyInteroperabilityRepresentationProfile`, `AocSovereigntyInteroperabilityProfileId`, `AocSovereigntyPortabilityMediaType`, `isValidSovereigntyInteroperabilityProfileV1`, `isValidSovereigntyInteroperabilityProfileRef`, `AOC_SOVEREIGNTY_CORE_SEMANTIC_VOCABULARY`, `AOC_SOVEREIGNTY_SEMANTIC_NAMESPACE`, `AOC_SOVEREIGNTY_SEMANTIC_TERMS`, `AOC_SOVEREIGNTY_SEMANTIC_TERM_IDS`, `AOC_SOVEREIGNTY_SEMANTIC_CATEGORIES`, `AOC_SOVEREIGNTY_SEMANTIC_CATEGORY_IDS`, `AOC_SOVEREIGNTY_SEMANTIC_VOCABULARY_ID`, `AocSovereigntySemanticTermId`, `getAocSovereigntySemanticTerm`, `SOVEREIGNTY_INTEROPERABILITY_ARTIFACT_KINDS`, `SovereigntyInteroperabilityArtifactKind`, `INTEROPERABLE_CLAIM_TYPES`, `InteroperableClaimType`, `INTEROPERABLE_STANDING_STATUSES`, `InteroperabilitySemanticRequirement`, `isSovereigntyInteroperabilityArtifactKind`, `isInteroperableClaimType`, `isInteroperableStandingStatus`, `isValidInteroperabilitySemanticRequirement`, `compareInteroperabilitySemanticRequirements`, `interoperabilitySemanticRequirementKey`, `SOVEREIGNTY_INTEROPERABILITY_DESCRIPTOR_SCHEMA_VERSION` (`aoc-sovereignty-interoperability-descriptor/1`), `SovereigntyInteroperabilityDescriptorV1`, `SovereigntyInteroperabilityDescriptorSchemaVersion`, `SovereigntyInteroperabilityPresentFeatures`, `buildSovereigntyInteroperabilityDescriptorV1`, `tryBuildSovereigntyInteroperabilityDescriptorV1`, `SovereigntyInteroperabilityDescriptorBuildResult`, `validateSovereigntyInteroperabilityDescriptorV1`, `isValidSovereigntyInteroperabilityDescriptorV1`, `SovereigntyInteroperabilityDescriptorValidationResult`, `presentInteroperabilityArtifactKinds`, `SOVEREIGNTY_INTEROPERABILITY_SUPPORT_SCHEMA_VERSION` (`aoc-sovereignty-interoperability-support/1`), `SovereigntyInteroperabilityConsumerSupportV1`, `SovereigntyInteroperabilitySupportSchemaVersion`, `SovereigntyInteroperabilityProfileSupport`, `buildSovereigntyInteroperabilityConsumerSupportV1`, `BuildSovereigntyInteroperabilityConsumerSupportV1Input`, `validateSovereigntyInteroperabilityConsumerSupportV1`, `isValidSovereigntyInteroperabilityConsumerSupportV1`, `SovereigntyInteroperabilityConsumerSupportValidationResult`, `SOVEREIGNTY_INTEROPERABILITY_REPORT_SCHEMA_VERSION` (`aoc-sovereignty-interoperability-report/1`), `SovereigntyInteroperabilityCompatibilityReportV1`, `SovereigntyInteroperabilityReportSchemaVersion`, `SOVEREIGNTY_INTEROPERABILITY_COMPATIBILITY_STATUSES`, `SovereigntyInteroperabilityCompatibilityStatus`, `SovereigntyInteroperabilityCoreCompatibility`, `assessSovereigntyInteroperabilityCompatibility`, `SOVEREIGNTY_INTEROPERABILITY_REASON_CODES`, `SovereigntyInteroperabilityReasonCode` | Stable | Mixed — frozen constant profile and vocabulary data, pure builders/validators, and the pure compatibility evaluator | Added post-0.1.0 (new subpath added in SM-07, additive/minor) | The self-describing layer around the canonical sovereign representation. `AOC_SOVEREIGNTY_INTEROPERABILITY_PROFILE_V1` is the static Protocol contract naming what a Soberanía representation family *means* — profile id and version, wire media type, the SM-06 bundle schema, the `aoc-canonical-json/1` profile, the artifact kinds, the claim semantics and the canonical sovereignty semantic vocabulary — so a receiving system can read every negotiable fact without inspecting Soberanía source. The profile version is deliberately distinct from the npm package version. `SovereigntyInteroperabilityDescriptorV1` describes one *concrete* bundle: which artifact kinds, manifest versions, claim types, standing statuses and semantic concepts are actually present. It duplicates no bundle payload and carries deliberately no `descriptorId` and no `describedAt`, so the same bundle always describes identically. Semantic requirements are extracted from `CanonicalSemanticRef` as `namespace` + `termRef` — concept identity, never the ref's occurrence id. The semantic vocabulary reuses the existing `CanonicalSemanticVocabulary`, `CanonicalSemanticCategory` and `CanonicalSemanticTerm` contracts rather than introducing a parallel model, and stays behaviour-free: it states meanings and evaluates, resolves, scores and decides nothing. `SovereigntyInteroperabilityConsumerSupportV1` is supplied explicitly by the receiving system — never inferred from a user-agent, package or runtime, and never fetched from a well-known URL, DID document, registry or DNS — and carries no consumer identity. Its validator fails closed on unknown vocabulary and on duplicates rather than silently cleaning a machine-readable contract. `assessSovereigntyInteroperabilityCompatibility` is pure and mutates neither argument, distinguishing *core* requirements (profile, media type, bundle schema, canonicalization profile) whose absence is `incompatible` from *feature* requirements whose absence is `partially-compatible`, with explicit sorted gaps and stable reason codes rather than any score. `partially-compatible` never authorizes data loss: nothing here strips, downgrades or projects a bundle. No signature is verified, no digest computed, no identity minted, no policy decided and no trust inferred; there is no external-standard translation (W3C VC, DID, C2PA, SPDX, JSON-LD), no adapter framework and no profile registry. `AOC_SOVEREIGNTY_PORTABILITY_MEDIA_TYPE` is a Soberanía Protocol media-type identifier and claims no IANA registration. Runtime dependencies are `@aoc/protocol/canonical`, `/identity`, `/manifest`, `/claims` and `/portability`; no import-time side effects. See `docs/protocol/SOVEREIGNTY_CAPABILITIES.md` |
| `@aoc/protocol/licensing` | `SOVEREIGN_LICENSE_TERMS_SCHEMA_VERSION` (`aoc-sovereign-license-terms/1`), `SovereignLicenseTermsV1`, `SovereignLicenseTermsSchemaVersion`, `SovereignLicenseTermsAudience` (`SovereignLicenseTermsPublicAudience` | `SovereignLicenseTermsPrincipalAudience` | `SovereignLicenseTermsCustomAudience`), `SovereignLicenseTermsAudienceKind`, `SOVEREIGN_LICENSE_TERMS_AUDIENCE_KINDS`, `SovereignLicenseTermsRuleV1`, `SovereignLicenseTermsRuleEffect`, `SOVEREIGN_LICENSE_TERMS_RULE_EFFECTS`, `SovereignLicenseActionRef`, `LicenseTermsClaim`, `BuildLicenseTermsClaimInput`, `buildLicenseTermsClaim`, `buildLicenseTermsSemanticRefs`, `licenseTermsSemanticConcepts`, `validateLicenseTermsClaim`, `isValidLicenseTermsClaim`, `validateSovereignLicenseTermsV1`, `isValidSovereignLicenseTermsV1`, `validateSovereignLicenseTermsAudience`, `isValidSovereignLicenseTermsAudience`, `validateSovereignLicenseTermsRuleV1`, `isValidSovereignLicenseTermsRuleV1`, `isValidSovereignLicenseTermsRuleEffect`, `validateSovereignLicenseActionRef`, `isValidSovereignLicenseActionRef`, `AOC_LICENSING_SEMANTIC_NAMESPACE`, `AOC_LICENSE_TERMS_SEMANTIC_VOCABULARY`, `AOC_LICENSE_TERMS_SEMANTIC_VOCABULARY_ID`, `AOC_LICENSING_SEMANTIC_TERMS`, `AOC_LICENSING_SEMANTIC_TERM_IDS`, `AOC_LICENSING_DECLARATION_TERM_IDS`, `AOC_LICENSING_ACTION_TERM_IDS`, `AOC_LICENSING_SEMANTIC_CATEGORIES`, `AOC_LICENSING_SEMANTIC_CATEGORY_IDS`, `AocLicensingSemanticTermId`, `AocLicensingDeclarationTermId`, `AocLicensingActionTermId`, `getAocLicensingSemanticTerm`, `isAocLicensingActionTermId`, `LICENSING_TERMS_REASON_CODES`, `LicensingTermsReasonCode`, `LicensingTermsValidationResult` | Stable | Mixed — frozen constant vocabulary data, pure builders and pure validators | Added post-0.1.0 (new subpath added in SM-09, additive/minor) | The structured, portable sovereign license terms model: what an issuer *declares* over a sovereign subject, in a form a machine can read, carry and describe. `LicenseTermsClaim` is a **specialized `AuthorityClaim`** with `metadata.kind === AuthorityClaimKind.License` and a versioned `metadata.terms` document — no `ClaimType.License` was added, `CanonicalClaim` is not forked, and `ClaimType.Authorization` is deliberately not the representation (it means "principal P is authorized to perform action A", a conclusion about an actor, while a declaration is a premise somebody else may later reason from). `SovereignLicenseTermsV1` carries a required audience, an optional issuer-supplied `effectiveAt` and a required, non-empty, order-preserving rule list; its `schemaVersion` is independent of the package, capability, manifest and bundle versions, and an unknown one fails closed. Every SM-09-owned structure uses exact-key validation, so an unknown field such as `automaticRoyaltyRate` is rejected rather than silently accepted and ignored; `AuthorityClaim.metadata` as a whole stays open. Rule effects are `Permission`/`Restriction`/`Obligation` and deliberately **not** `Allow`/`Deny`, which would name the output of a runtime decision. Action references are open-world `(namespace, termRef)` concept pairs: inside the Protocol-owned `aoc.licensing` namespace a term must be one of this version's canonical action concepts (a misspelling is rejected), and any other namespace is preserved opaquely and **never** dereferenced — no HTTP, ontology registry, DID, DNS, JSON-LD context or semantic resolver exists here. Clause `statement`s are inert data, never parsed into policy; there is no condition language (no `and`/`or`/`not`, operator, expression tree, CEL, Rego, Cedar, JSON Logic or XACML). Duplicate clause ids are reported rather than deduplicated, sparse rule arrays are rejected, and caller-authored rule order is preserved exactly. Generated `semanticRefs` are ordinary `CanonicalSemanticRef`s with deterministic `<claimId>:semantic:<n>` ids, deduplicated by concept identity, so the unchanged SM-07 machinery discovers licensing semantics — including external ones — generically. `aoc.licensing` is a separate vocabulary: the SM-07 core `aoc.sovereignty` vocabulary, profile and descriptor schema are untouched. Contradictory clauses are representable and **no precedence** is applied; no wall clock is consulted, so no `isActive`/`isExpiredNow` exists and no `StandingStatus` is derived from a date. Nothing here signs, verifies, mints identity, requires bytes, creates provenance, inherits terms across derivation, concludes ownership or legality, prices, meters, invoices or settles anything, and nothing reaches a filesystem, network, database, chain, provider or registry. Runtime dependencies are `@aoc/protocol/claims` primitives, `@aoc/protocol/identity` and `@aoc/protocol/manifest`; no import-time side effects. See `docs/protocol/LICENSING_TERMS.md` |
| `@aoc/protocol/governance-compatibility` | `SOVEREIGN_GOVERNANCE_HANDOFF_SCHEMA_VERSION` (`aoc-sovereign-governance-handoff/1`), `SovereignGovernanceHandoffV1`, `SovereignGovernanceHandoffSchemaVersion`, `BuildSovereignGovernanceHandoffInput`, `SovereignGovernanceHandoffBuildResult`, `SovereignGovernanceHandoffValidationResult`, `buildSovereignGovernanceHandoffV1`, `tryBuildSovereignGovernanceHandoffV1`, `validateSovereignGovernanceHandoffV1`, `isValidSovereignGovernanceHandoffV1`, `sovereignGovernanceSubjectsEqual`, `SOVEREIGN_GOVERNED_RESOURCE_KIND` (`aoc:sovereign-asset`), `SovereignGovernedResourceKind`, `buildSovereignGovernanceResourceRef`, `BuildSovereignGovernanceResourceRefOptions`, `SOVEREIGN_GOVERNANCE_COMPATIBILITY_REASON_CODES`, `SovereignGovernanceCompatibilityReasonCode` | Stable | Pure — one frozen constant, one pure projection, one pure builder and one pure validator | Added post-0.1.0 (new subpath added in SM-10, additive/minor) | The sovereign governance handoff: the deterministic, provider-neutral boundary object an external governance system receives, and the point at which Soberanía Protocol stops. `SovereignGovernanceHandoffV1` has exactly six top-level fields — `schemaVersion`, `canonicalizationProfile`, `subject`, `resource`, `representation`, `semantics` — and the envelope is closed, so an unknown field such as `policy`, `decision`, `grant`, `owner`, `authority`, `approval` or `governanceReady` is rejected rather than ignored. `subject` is the canonical SM-02 `SovereignSubjectRef` itself, `resource` the canonical `ResourceRef` from `@aoc/protocol/contracts`, `representation` the unchanged SM-06 `SovereigntyPortabilityBundleV1`, and `semantics` the SM-07 `SovereigntyInteroperabilityDescriptorV1` — every one reused rather than re-declared, so no parallel `GovernedSubject`, `GovernedResourceRef` or `GovernanceBundleV1` exists. There is deliberately no `handoffId`, `generatedAt`, `handoffDigest` or `signature`: the handoff is a deterministic projection of an existing subject, not a new sovereign object, and integrity or proof over it is explicit composition with AOC.INTEGRITY and AOC.VERIFIABILITY over its canonical serialization. The resource projection is fixed — `kind === SOVEREIGN_GOVERNED_RESOURCE_KIND` and `id === subject.sovereignAssetId`, never a manifest digest, `ContentIdentity.digest`, external-reference id, locator, CID, URL, provider id, database id, token address or registry record id, because a subject that changes provider, locator, bytes or manifest version is still the same sovereign subject and governance must address the sovereignty anchor. `attributes` is structurally absent in v1 and a handoff carrying it is invalid, so no `assetType`, `jurisdiction`, `classification`, `owner`, `license` or `riskScore` can be smuggled into governance through it. `tenantId` is optional, preserved verbatim when supplied, rejected when blank, and **never** inferred or defaulted to `'default'`, `'public'`, a subject namespace, a registrant, an issuer or an environment variable — a specialized consumer such as the Asset Protocolization vertical may require one, and narrowing a generic Protocol contract is where that constraint belongs. `SOVEREIGN_GOVERNED_RESOURCE_KIND` is the single Protocol-owned definition of the generic sovereign-resource kind, promoted here from the APV-02 vertical-owned `PROTOCOLIZED_RESOURCE_KIND` now that a second, generic producer exists; the dependency direction is unchanged (Asset Protocolization may consume Protocol, never the reverse). Validation proves that `semantics` is **exactly** the canonical descriptor derived from `representation` with SM-07's own pure helper — a descriptor that is individually well-formed but describes a different bundle is invalid — and that one subject is shared by the envelope, the representation and the descriptor under exact SM-02 equality. Structural validity is deliberately not policy sufficiency: a valid handoff may carry zero claims, no licence terms, unsigned artifacts, contested standings, a permission and a restriction over the same action, and cryptographic proofs that do not hold. Nothing here evaluates policy, returns allow/deny, resolves contradictory terms, derives a scope, produces a `PolicyDecision`, `ScopedAccessRequest`, `CapabilityToken`, `CapabilityGrant`, `ConsentGrant`, `Delegation`, `CanonicalCapability`, `CanonicalAuthority` or `CanonicalDecision`, infers ownership or authority from a registrant, issuer or signature, verifies a proof, hashes content, mints identity, approves, enforces, revokes, prices or persists anything. Runtime dependencies are `@aoc/protocol/canonical`, `/contracts`, `/identity`, `/portability` and `/interoperability`; no import-time side effects. See `docs/protocol/GOVERNANCE_COMPATIBILITY.md` |
| `@aoc/protocol/sovereignty-capabilities` | `SOVEREIGNTY_CAPABILITY_NAMESPACE`, `SOVEREIGNTY_CAPABILITY_KEYS`, `SOVEREIGNTY_CAPABILITY_IDS`, `SOVEREIGNTY_CAPABILITIES`, `listSovereigntyCapabilities`, `getSovereigntyCapability`, `getSovereigntyCapabilityByKey`, `isSovereigntyCapabilityId`, `isSovereigntyCapabilityKey`, `isSovereigntyCapabilityVersion`, `SovereigntyCapabilityId`, `SovereigntyCapabilityKey`, `SovereigntyCapabilityNamespace`, `SovereigntyCapabilityVersion`, `SovereigntyCapabilityDefinition`, `SovereigntyCapabilityRef`, `toSovereigntyCapabilityRef`, `getSovereigntyCapabilityRef`, `getSovereigntyCapabilityRefByKey`, `isValidSovereigntyCapabilityRef`, `sovereigntyCapabilityRefsEqual`, `SovereigntyCapabilityInvocationId`, `mintSovereigntyCapabilityInvocationId`, `isValidSovereigntyCapabilityInvocationId`, `SOVEREIGNTY_CAPABILITY_INVOCATION_SCHEMA_VERSION`, `SovereigntyCapabilityInvocation`, `SovereigntyCapabilityInvocationSchemaVersion`, `SovereigntyCapabilityInvocationValidationResult`, `BuildSovereigntyCapabilityInvocationInput`, `buildSovereigntyCapabilityInvocation`, `validateSovereigntyCapabilityInvocation`, `isValidSovereigntyCapabilityInvocation`, `SovereigntyCapabilityImplementation`, `SovereigntyCapabilityExecutionOutcome`, `SovereigntyCapabilitySuccessOutcome`, `SovereigntyCapabilityFailureOutcome`, `isValidSovereigntyCapabilityExecutionOutcome`, `SOVEREIGNTY_CAPABILITY_RESULT_SCHEMA_VERSION`, `SovereigntyCapabilityResult`, `SovereigntyCapabilitySuccessResult`, `SovereigntyCapabilityFailureResult`, `SovereigntyCapabilityResultSchemaVersion`, `resolveSovereigntyCapabilitySubject`, `SOVEREIGNTY_CAPABILITY_INVOCATION_EVIDENCE_SCHEMA_VERSION`, `SOVEREIGNTY_CAPABILITY_INVOCATION_EVENT_TYPE`, `SovereigntyCapabilityInvocationEvidenceV1`, `SovereigntyCapabilityInvocationEvidenceSchemaVersion`, `SovereigntyCapabilityInvocationOutcomeStatus`, `isValidSovereigntyCapabilityInvocationEvidence`, `toSovereigntyCapabilityInvocationAuditEvent`, `invokeSovereigntyCapability`, `InvokeSovereigntyCapabilityOptions`, `SovereigntyCapabilityClock`, `SovereigntyCapabilityInvocationError`, `isSovereigntyCapabilityInvocationError`, `SOVEREIGNTY_CAPABILITY_INVOCATION_ERROR_CODES`, `SovereigntyCapabilityInvocationErrorCode`, `SovereigntyCapabilityInvocationErrorDetails`, `CanonicalEvidenceId` (re-export), `createIdentitySovereigntyCapabilityImplementation`, `IdentitySovereigntyCapabilityImplementation`, `IdentitySovereigntyCapabilityInput`, `IdentitySovereigntyCapabilityOutput`, `IdentitySovereigntyCapabilityInputValidationResult`, `IdentitySovereigntyCapabilityReasonCode`, `CreateIdentitySovereigntyCapabilityImplementationOptions`, `validateIdentitySovereigntyCapabilityInput`, `isValidIdentitySovereigntyCapabilityInput`, `IDENTITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `createIntegritySovereigntyCapabilityImplementation`, `IntegritySovereigntyCapabilityImplementation`, `IntegritySovereigntyCapabilityInput`, `IntegritySovereigntyCapabilityOutput`, `IntegritySovereigntyCapabilityInputValidationResult`, `IntegritySovereigntyCapabilityReasonCode`, `IntegritySovereigntyCapabilityOperation`, `ComputeContentIdentityIntegrityInput`, `ComputeContentIdentityIntegrityOutput`, `VerifyContentIdentityIntegrityInput`, `VerifyContentIdentityIntegrityOutput`, `ComputeManifestDigestIntegrityInput`, `ComputeManifestDigestIntegrityOutput`, `IntegrityContentIdentityCheck`, `validateIntegritySovereigntyCapabilityInput`, `isValidIntegritySovereigntyCapabilityInput`, `INTEGRITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `INTEGRITY_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `createProvenanceSovereigntyCapabilityImplementation`, `ProvenanceSovereigntyCapabilityImplementation`, `ProvenanceSovereigntyCapabilityInput`, `ProvenanceSovereigntyCapabilityOutput`, `ProvenanceSovereigntyCapabilityInputValidationResult`, `ProvenanceSovereigntyCapabilityReasonCode`, `ProvenanceSovereigntyCapabilityOperation`, `CreateProvenanceSovereigntyCapabilityImplementationOptions`, `DeclareOriginProvenanceInput`, `DeclareOriginProvenanceOutput`, `DeclareAuthorshipProvenanceInput`, `DeclareAuthorshipProvenanceOutput`, `RecordDerivationProvenanceInput`, `RecordDerivationProvenanceOutput`, `ContestProvenanceClaimInput`, `ContestProvenanceClaimOutput`, `TraceLineageProvenanceInput`, `TraceLineageProvenanceOutput`, `validateProvenanceSovereigntyCapabilityInput`, `isValidProvenanceSovereigntyCapabilityInput`, `PROVENANCE_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `PROVENANCE_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `createPortabilitySovereigntyCapabilityImplementation`, `PortabilitySovereigntyCapabilityImplementation`, `PortabilitySovereigntyCapabilityInput`, `PortabilitySovereigntyCapabilityOutput`, `PortabilitySovereigntyCapabilityInputValidationResult`, `PortabilitySovereigntyCapabilityReasonCode`, `PortabilitySovereigntyCapabilityOperation`, `ExportPortabilityBundleInput`, `ExportPortabilityBundleOutput`, `ImportPortabilityBundleInput`, `ImportPortabilityBundleOutput`, `validatePortabilitySovereigntyCapabilityInput`, `isValidPortabilitySovereigntyCapabilityInput`, `PORTABILITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `PORTABILITY_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `createInteroperabilitySovereigntyCapabilityImplementation`, `InteroperabilitySovereigntyCapabilityImplementation`, `InteroperabilitySovereigntyCapabilityInput`, `InteroperabilitySovereigntyCapabilityOutput`, `InteroperabilitySovereigntyCapabilityInputValidationResult`, `InteroperabilitySovereigntyCapabilityReasonCode`, `InteroperabilitySovereigntyCapabilityOperation`, `DescribeInteroperabilityBundleInput`, `DescribeInteroperabilityBundleOutput`, `AssessInteroperabilityCompatibilityInput`, `AssessInteroperabilityCompatibilityOutput`, `validateInteroperabilitySovereigntyCapabilityInput`, `isValidInteroperabilitySovereigntyCapabilityInput`, `INTEROPERABILITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `INTEROPERABILITY_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `createVerifiabilitySovereigntyCapabilityImplementation`, `VerifiabilitySovereigntyCapabilityImplementation`, `VerifiabilitySovereigntyCapabilityInput`, `VerifiabilitySovereigntyCapabilityOutput`, `VerifiabilitySovereigntyCapabilityInputValidationResult`, `VerifiabilitySovereigntyCapabilityReasonCode`, `VerifiabilitySovereigntyCapabilityOperation`, `CreateVerifiabilitySovereigntyCapabilityImplementationOptions`, `VerifySignedSovereignManifestInput`, `VerifySignedSovereignManifestOutput`, `VerifySignedSovereignClaimInput`, `VerifySignedSovereignClaimOutput`, `VerifySovereignProofInput`, `VerifySovereignProofOutput`, `SovereignProofVerificationResult`, `validateVerifiabilitySovereigntyCapabilityInput`, `isValidVerifiabilitySovereigntyCapabilityInput`, `VERIFIABILITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `VERIFIABILITY_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `createLicensingTermsSovereigntyCapabilityImplementation`, `LicensingTermsSovereigntyCapabilityImplementation`, `LicensingTermsSovereigntyCapabilityInput`, `LicensingTermsSovereigntyCapabilityOutput`, `LicensingTermsSovereigntyCapabilityInputValidationResult`, `LicensingTermsSovereigntyCapabilityOperation`, `CreateLicensingTermsSovereigntyCapabilityImplementationOptions`, `DeclareLicenseTermsInput`, `DeclareLicenseTermsOutput`, `ValidateLicenseTermsInput`, `ValidateLicenseTermsOutput`, `ContestLicenseTermsClaimInput`, `ContestLicenseTermsClaimOutput`, `validateLicensingTermsSovereigntyCapabilityInput`, `isValidLicensingTermsSovereigntyCapabilityInput`, `LICENSING_TERMS_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `LICENSING_TERMS_REASON_CODES` (re-export), `LicensingTermsReasonCode` (re-export), `LicensingTermsValidationResult` (re-export), `createGovernanceCompatibilitySovereigntyCapabilityImplementation`, `GovernanceCompatibilitySovereigntyCapabilityImplementation`, `GovernanceCompatibilitySovereigntyCapabilityInput`, `GovernanceCompatibilitySovereigntyCapabilityOutput`, `GovernanceCompatibilitySovereigntyCapabilityInputValidationResult`, `GovernanceCompatibilitySovereigntyCapabilityOperation`, `GovernanceCompatibilitySovereigntyCapabilityReasonCode`, `PrepareGovernanceHandoffInput`, `PrepareGovernanceHandoffOutput`, `ValidateGovernanceHandoffInput`, `ValidateGovernanceHandoffOutput`, `validateGovernanceCompatibilitySovereigntyCapabilityInput`, `isValidGovernanceCompatibilitySovereigntyCapabilityInput`, `GOVERNANCE_COMPATIBILITY_SOVEREIGNTY_CAPABILITY_OPERATIONS`, `GOVERNANCE_COMPATIBILITY_SOVEREIGNTY_CAPABILITY_REASON_CODES`, `SOVEREIGN_GOVERNANCE_COMPATIBILITY_REASON_CODES` (re-export), `SovereignGovernanceCompatibilityReasonCode` (re-export) | Stable | Mixed — frozen constant data, pure lookup functions, and the invocation runtime | Added post-0.1.0 (invocation & evidence spine added in SM-03; production Identity and Integrity capsules added in SM-04; production Provenance capsule added in SM-05; production Portability capsule added in SM-06; production Interoperability capsule added in SM-07; production Verifiability capsule added in SM-08; production Licensing & Terms capsule added in SM-09; production Governance Compatibility capsule added in SM-10 — all additive/minor) | Canonical identity, version and discovery for the eight Sovereignty Capabilities, the common contract for invoking one and producing portable capability-attributed evidence, and all eight production capsules. The inventory stays read-only: there is no registration or mutation API, and no global implementation registry — `invokeSovereigntyCapability` takes its implementation as an explicit argument, and the capsule factories have no import-time side effects. **All eight** (`AOC.IDENTITY`, `AOC.INTEGRITY`, `AOC.PROVENANCE`, `AOC.PORTABILITY`, `AOC.INTEROPERABILITY`, `AOC.VERIFIABILITY`, `AOC.LICENSING_TERMS`, `AOC.GOVERNANCE_COMPATIBILITY`) now ship production implementations; the canonical inventory is closed at eight and SM-10 adds no ninth mineral. Capability versions are unchanged at `1.0.0` — adding an implementation is not a capability-contract change. Provenance requires an existing subject and creates none, never signs or verifies, never mutates a manifest, and inherits nothing along a derivation edge; its lineage traversal is pure over caller-supplied claims with no database or provider. Portability exports and imports the canonical bundle defined by `@aoc/protocol/portability`: `export-bundle` requires an existing subject and never mints one, `import-bundle` works with **no** invocation subject and returns the existing subject that arrived in the bundle, and an explicitly supplied import subject must match it exactly rather than being reconciled. Import is not persistence, registration, verification or trust — nothing is registered, stored, signed, verified, fetched or dereferenced — and the generic evidence carries neither the bundle nor its serialized form. Interoperability describes a bundle and assesses it against a consumer's declared support: `describe-bundle` also works with **no** invocation subject and returns the bundle's existing subject, and an incompatible or partial compatibility report is an ordinary *successful* execution — a capability failure is reserved for input that cannot be read at all. It reuses the Portability bundle contract and validator but never invokes the Portability capsule, verifies no signature, computes no digest, mints no identity, drops no artifact, translates to no external standard and carries no trust, policy or ownership semantics; its generic evidence carries neither the profile, the descriptor, the support declaration nor the report. Verifiability independently checks supplied proofs through three operations — `verify-signed-manifest`, `verify-signed-claim` and `verify-sovereign-proof` — reusing `verifySovereignManifest`, `verifySignedSovereignClaim`/`verifySignedClaim` and `verifySovereignSignature` as the owning cryptographic primitives, so no second canonicalizer, digest or Ed25519 implementation exists and no new algorithm is introduced. It is **verification-first**: no signing or key-generation operation exists, no private key field exists in its input contract in any spelling, and proof issuance stays with the low-level `@aoc/protocol/manifest` primitives. Each check is reported individually rather than as one boolean, and `verify-signed-manifest` deliberately accepts **no** content bytes — the resulting `contentDigest: 'not_performed'` is the Integrity boundary reported honestly rather than silently upgraded to `valid`. Issuer/key binding is optional and three-state (`verified` / `unverified` / `not_performed`) through an *injected* `VerificationKeyResolver`; without one the binding is `not_performed`, a non-matching key is `unverified`, and a resolver fault is a failed execution (`VERIFIABILITY_KEY_RESOLUTION_FAILED`) with exactly one attempt, no retry and no leaked exception text. A cryptographically invalid artifact is an ordinary *successful* execution with an invalid report; capability failure is reserved for input that cannot be read at all. Signature validity establishes neither historical truth, legal ownership, authorization, licence validity, trust nor a governance decision; issuer binding is a key relationship and not identity, ownership or non-revocation; no revocation, certificate chain, DID resolution, wallet signature profile or key-validity-window policy is performed; no trust or confidence score exists; no `CanonicalVerification` is created and no `VerificationStatus` member is added or assigned; claim standing is never read or mutated, so a valid signature and `Contested` standing coexist. Nothing is mutated, repaired or re-signed, and the deterministic report carries no id or timestamp. Licensing & Terms declares, validates and contests structured sovereign license terms defined by `@aoc/protocol/licensing`, through three operations — `declare-license-terms`, `validate-license-terms` and `contest-license-terms-claim`. `declare-license-terms` requires an existing subject, accepts no `sovereignAssetId` of its own so a claim cannot disagree with the invocation, never mints identity and requires no content bytes. An invalid *candidate* under `validate-license-terms` is an ordinary **successful** execution reporting `valid: false` — capability failure is reserved for input that cannot be read at all, so a malformed *declare* request fails and produces no partial claim; validation also works with **no** invocation subject, and never fabricates one for an unreadable candidate. It reuses `buildAuthorityClaim` and `contestClaim` and defines no parallel claim hierarchy. There is deliberately **no** `evaluate-license`, `is-action-permitted`, `isAllowed`, `isDenied`, `authorize-use`, `canUse`, `canDistribute`, `canDerive`, `check-obligation` or `supersede-license-terms` operation, and no condition language to write one with: a declared permission is not a runtime grant, a declared restriction is not an enforced denial, and a declared obligation is not proof of compliance. No precedence is applied between contradictory clauses or between multiple declarations over one subject, no wall clock decides whether terms are in force, and no `StandingStatus` other than `Contested` is ever produced. It never signs (no private key field exists in its input contract in any spelling), never verifies, never creates provenance, never inherits terms across a derivation edge, never reads a manifest's `registrant` as the licensing issuer, and concludes nothing about ownership, legality, pricing, royalties or settlement. A cryptographically valid signature over a structurally malformed terms document is an ordinary, representable pair, and a valid signature coexists with `Contested` standing. `SovereigntyPortabilityBundleV1` is unchanged — a licensing claim travels in the existing `claims` array as `claim`/`signed-claim` — and the SM-07 profile, descriptor schema and core vocabulary are unchanged too. Its generic evidence carries no rule, statement, audience, claim or semantic-ref payload. Governance Compatibility closes the shelf: it projects a sovereign representation into the `SovereignGovernanceHandoffV1` defined by `@aoc/protocol/governance-compatibility`, through two operations — `prepare-governance-handoff` and `validate-governance-handoff`. `prepare-governance-handoff` works with **no** invocation subject and returns the existing subject the representation arrived with; an explicitly supplied subject must match it exactly and a mismatch fails without producing a handoff. It accepts no `actor`, `principal`, `action`, `scope`, `policy`, `authority`, `grant`, `decision`, `owner` or credential field, and its output carries the handoff and nothing else — no `ready`, `governable`, `complete` or `sufficient` flag, because the Protocol cannot know what artifacts exist beyond the ones it was handed or what a policy it has never seen requires. An invalid *candidate* under `validate-governance-handoff` is an ordinary **successful** execution reporting `valid: false`, and no subject is fabricated from an unreadable one; a mismatching explicit subject over a *valid* handoff is an attribution failure instead. It reuses the SM-06 bundle validator and the SM-07 descriptor helper as pure libraries and never invokes the Portability or Interoperability capsule — `invokeSovereigntyCapability` is not called anywhere in it — so one prepare produces exactly one evidence record rather than a hidden chain of them, and a caller may prepare a handoff directly without running Interoperability first. There is deliberately **no** `evaluate`, `authorize`, `decide`, `approve`, `grant` or `enforce` operation. It verifies no signature (a representation carrying a tampered proof still prepares successfully, while AOC.VERIFIABILITY independently reports `valid: false` for the same artifact), resolves no contested standing, picks no winner between a declared permission and a declared restriction over the same action, turns no `Permission` into a grant or scope, no `Restriction` into a deny and no `Obligation` into a compliance status, and infers no owner or authority from a registrant, claim issuer, licence issuer or valid signature. `SovereigntyPortabilityBundleV1`, the SM-07 profile and descriptor schema and the core `aoc.sovereignty` vocabulary are all unchanged — the handoff *wraps* Portability rather than extending it, and no `governance-handoff` artifact kind exists, so a handoff can never contain a representation containing a handoff. Its generic evidence carries no handoff, resource, representation, descriptor, tenant or terms payload. Runtime dependencies are `@aoc/protocol/identity`, `@aoc/protocol/manifest`, `@aoc/protocol/canonical`, `@aoc/protocol/licensing` (capsules only), `@aoc/protocol/governance-compatibility` (capsules and the evidence audit-envelope projection only), `@aoc/protocol/claims` primitives (capsules only) and `node:crypto` (`randomUUID`); `AuditEventSink`, `AuditEventEnvelope`, `CanonicalEvidenceId`, `ResourceRef`/`CanonicalId` and `VerificationKeyResolver` are type-only imports. See `docs/protocol/SOVEREIGNTY_CAPABILITIES.md`, `docs/protocol/LICENSING_TERMS.md` and `docs/protocol/GOVERNANCE_COMPATIBILITY.md` |

### Packaged metadata that is deliberately not an export

The tarball also ships `integration-contract.json` at its root — the frozen cross-repository
integration contract (`aoc.cross-repository-integration`), described in
[`../integration/CROSS_REPO_INTEGRATION_CONTRACT.md`](../integration/CROSS_REPO_INTEGRATION_CONTRACT.md).
It is **not** in this table and **not** in `exports`, by decision: it is package metadata in the same
class as `LICENSE` and `NOTICE`, read by path
(`node_modules/@aoc/protocol/integration-contract.json`) by a consumer's CI verification step, never
imported by application code. `@aoc/protocol/integration-contract.json` does not resolve, and a
consumer fixture asserts that it does not.

Promoting it to a declared export would be an additive public-API change under the governance policy
above — a `minor` Changeset plus a row in this table — and requires its own approved public-API
decision. `npm run check:integration-contract` fails if it appears in `exports` without one.

## Governance decisions (Public API Stabilization Sprint)

- **`AocIdentityClaims` is intentionally not part of this API.** Soberanía Enterprise's local ambient
  shim (`types/aoc-protocol/index.d.ts`) invented this symbol; it was never exported by any Protocol
  subpath and has no equivalent in Protocol's git history. Protocol's principal-identity surface is,
  by design (see `docs/audits/principal-reference-source-audit.md`), limited to non-verifying
  *references* (`@aoc/protocol/claims` `CanonicalPrincipalRef`) — verified identity/auth claims
  (e.g. token subject claims) are an implementation-specific concern and belong in the consuming
  product, not in Protocol. Enterprise's migration off this import is tracked as separate,
  Enterprise-side follow-up work.
- **`ScopedAccessRequest`'s shape (`principalId`, `resource`, `requestedScope`, `requestedAt`) is
  unchanged and has been canonical since the type's introduction** — there is no historical
  `scope`/`action` shape to preserve compatibility with.
- **`AuditEventEnvelope` gained five additive, optional fields** (`occurredAt`, `subject`,
  `correlationId`, `reasonCodes`, `schemaVersion`) to make the envelope a viable target for
  consumers migrating from richer, product-specific audit event shapes (e.g. Enterprise's
  `event_id`/`occurred_at`/`subject_id`/`requester_id`/`request_id`/`reason` fields). `actorId`
  represents the acting/requesting principal, not the subject; `subject` (new) represents the
  entity the event is about, reusing `ResourceRef` rather than introducing a new shape.

## Verified consumer coverage

Every subpath in the table above is exercised end-to-end (installed from a real `npm pack` tarball,
compiled, and executed) by `npm run protocol:consumer:check` against the fixtures in
`test-consumers/`. See `docs/versioning-and-stability.md` for the CJS/ESM compatibility evidence.
